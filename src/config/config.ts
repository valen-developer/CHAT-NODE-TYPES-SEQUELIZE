export const DBCONFIG = {
  database: process.env.DBNAME || "chat",
  user: process.env.DBUSER || "valen",
  password: process.env.DBPASSWORD || "dbpass",
  host: process.env.DBHOST || "localhost",  
};
